{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Debug TypeScript",
      "program": "${workspaceFolder}/src/main.ts",  // Your entry file
      "outFiles": ["${workspaceFolder}/dist/**/*.js"], // Make sure this matches your outDir
      "preLaunchTask": "npm: build",
      "sourceMaps": true,
      "args": [
        "testing"
      ],
    }
  ]
}
